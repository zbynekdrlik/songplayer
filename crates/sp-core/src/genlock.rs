//! Genlock wall-clock boundary math (#146). Implementation follows in the
//! GREEN commit; this shell only wires the RED reference-vector tests.

#[cfg(test)]
#[path = "genlock_tests.rs"]
mod genlock_tests;
