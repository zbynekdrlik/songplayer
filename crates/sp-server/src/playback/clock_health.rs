//! dantesync clock-health polling + evaluation (#146). Implementation
//! follows in the GREEN commit; this shell only wires the RED tests.

#[cfg(test)]
#[path = "clock_health_tests.rs"]
mod clock_health_tests;
