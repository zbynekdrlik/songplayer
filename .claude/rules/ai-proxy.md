---
paths:
  - "crates/sp-server/src/ai/**"
  - "crates/sp-core/src/config.rs"
---
# CLIProxyAPI (Claude via OAuth) — lifecycle + model default

Full ops (installed version, upgrade/rollback/probe procedures, config schema):
**`scripts/cliproxy/README.md`**. Read it before touching the proxy on win-resolume.

## Lifecycle — it is SongPlayer's own child, not a service

- `sp_server::ai::proxy::ProxyManager::start()` spawns `CLIProxyAPI.exe -config <cfg>`
  OR **adopts** an already-healthy instance on port 18787 (`probe_port` → 200 → no spawn).
- `ai_proxy_watchdog` (`lib.rs`, 30 s) restarts it if it goes down. There is **no**
  Windows service / scheduled task — do not create one.
- `write_config` regenerates `cli-proxy-api-config.yaml` on **every** (re)spawn, so
  hand-edits on the box are overwritten — change `write_config` in code instead.
  The config is generated by the *currently deployed* SongPlayer, so a proxy binary
  swapped in ahead of a deploy must accept the config the deployed `write_config` emits.
- To restart the proxy on the box: replace the exe / kill the PID and let the watchdog
  respawn it (it self-heals; the lyrics worker's retry/backoff covers the ≤30 s gap).
  Never kill OBS / Resolume / SongPlayer.

## Config schema (CLIProxyAPI 7.x)

7.x dropped the top-level `claude-oauth` block (6.9.x used it to force `cloak: never`);
cloak is now default-on (disabled only via `disable-claude-cloak-mode`), which is the
safer mode for Claude OAuth. `write_config` no longer emits the block; 7.x ignores its
absence. `auth-dir` / `host` / `port` / `request-retry` / `claude-api-key` unchanged.

## Model default (`sp-core/config.rs::DEFAULT_AI_MODEL`)

- The lyrics worker sends `DEFAULT_AI_MODEL` (overridable by the `ai_model` setting) to
  `{api_url}/chat/completions`. It must be an id the proxy's `/v1/models` actually lists,
  or the request fails `502 unknown provider` at the routing layer (no upstream call).
- Owner directive: **newest flagship only, verified AS OF TODAY**. Probe a candidate with
  `python C:\ProgramData\SongPlayer\proxy_probe.py <model>` on win-resolume before changing
  it. **Never** probe an unlisted id — a `404` from Anthropic parks the OAuth auth in a
  cooldown until the proxy restarts (#144).
- Current: `claude-fable-5-1` (CLIProxyAPI 7.3.1, #145). Pinned by the `config.rs` test.
- **Refusals are a PROMPT problem, not a model problem (#145, 2026-09-14).** The
  newest flagships (`claude-fable-5-1`, `claude-opus-5`) refuse a "story"-framed
  translation prompt on recognizable content ("…even for a family plaque…"); a
  bare neutral technical prompt translates cleanly on all of them (measured live,
  9/9). So a refusal is fixed in `translator::build_prompt`, not by downgrading
  the model — see the `lyrics-pipeline` skill "Translation" section. A refusal is
  now classified + logged `kind="refusal"` with the model id
  (`translator::classify_zero_translation`), so it is never a silent "parse
  returned 0" again.
