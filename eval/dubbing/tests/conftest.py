"""Shared pytest setup for eval/dubbing tests.

Empty — package imports work via the __init__.py files in eval/, eval/dubbing/
and eval/dubbing/tests/. Present so pytest treats this as a rootdir-aware suite.
"""
