"""Shared pytest setup for eval/lyrics tests."""

# Empty — package imports work via the __init__.py files in eval/ and eval/lyrics/.
# Keeping this file present so pytest treats eval/lyrics/tests as a rootdir-aware suite.
